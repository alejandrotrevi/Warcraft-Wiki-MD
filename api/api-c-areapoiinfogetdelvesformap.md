# API C AreaPoiInfo.GetDelvesForMap

**Contributor:** KethoBot

## Content

The following content is in MediaWiki markdown format:

```mediawiki
{{wowapi|t=a|namespace=C_AreaPoiInfo|system=AreaPoiInfo}}
Returns all area POIInfos flagged as delves for the given map.
 areaPoiIDs = C_AreaPoiInfo.GetDelvesForMap(uiMapID)

==Arguments==
:;uiMapID:{{apitype|number}}

==Returns==
:;areaPoiIDs:{{apitype|number[]}}
```