# API C ChallengeMode.GetKeystoneLevelRarityColor

**Contributor:** KethoBot

## Content

The following content is in MediaWiki markdown format:

```mediawiki
{{wowapi|t=a|namespace=C_ChallengeMode|system=ChallengeModeInfo}}
Returns a color value from the passed in keystone level.
 levelScore = C_ChallengeMode.GetKeystoneLevelRarityColor(level)

==Arguments==
:;level:{{apitype|number}}

==Returns==
:;levelScore:{{apitype|ColorMixin}}

==Patch changes==
* {{Patch 9.1.0|note=Added.}}
```