# API IsPetAttackActive

**Contributor:** KethoBot

## Content

The following content is in MediaWiki markdown format:

```mediawiki
{{wowapi}}
Returns true if the pet is currently auto attacking.
 isActive = IsPetAttackActive()

==Returns==
:;isActive:{{apitype|boolean}}
```